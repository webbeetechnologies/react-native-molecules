---
title: "IconButton"
description: "Compact tappable icon with ripple + state layer feedback."
sidebar_position: 26
---

import ComponentDoc from '@site/src/components/ComponentDoc';
import ComponentProps from '@site/src/components/ComponentProps';
import LiveCodeExample from '@site/src/components/LiveCodeExample';

<ComponentDoc name="IconButton" />

## Examples

### Default

<LiveCodeExample
    code={`import { IconButton } from 'react-native-molecules/components/IconButton';

export default function Example() {
    return <IconButton name="home-outline" onPress={() => console.log('Pressed')} />;
}`}
/>

### Shape and width

Material 3 icon buttons support round or square containers and narrow, default, or wide widths.

<LiveCodeExample
    code={`import { View } from 'react-native';
import { IconButton } from 'react-native-molecules/components/IconButton';

export default function Example() {
    return (
        <View style={{ flexDirection: 'row', gap: 12, alignItems: 'center' }}>
            <IconButton name="heart-outline" variant="contained-tonal" shape="round" width="narrow" />
            <IconButton name="heart-outline" variant="contained-tonal" shape="square" />
            <IconButton name="heart-outline" variant="contained-tonal" shape="round" width="wide" />
            <IconButton name="star-outline" variant="outlined" shape="square" width="wide" size={32} />
        </View>
    );
}`}
/>

<ComponentProps name="IconButton" />
